import { AddButtonContainer } from "@/components/RoadMap/index.styles";
import AddIcon from "@mui/icons-material/Add";

export function AddButton() {
  return (
    <AddButtonContainer>
      <AddIcon />
      {"Add"}
    </AddButtonContainer>
  );
}
